from lib.math import dot, transpose, PI, E, abs, pow, sqrt, exp, log, sin, cos, tan, factorial
from lib.activations import relu, sigmoid
from lib.losses import mse, mse_grad

# Constantes disponibles para el visitor
constants = {
    "PI": PI,
    "E": E
}
